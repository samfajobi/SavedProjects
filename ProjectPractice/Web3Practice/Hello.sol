//SPDX-License-Identifier: MIT;



contract practice {
    
    // min and max is used to prevent underflow and overflow.
    int public minInt = type(int).min;
    int public maxInt = type(int).max;
}


contract deployContract {
    function run() external returns(sim)
}